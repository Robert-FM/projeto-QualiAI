[project]
name = "projeto-qualiai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Robert-FM", email = "robertfmelo29@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.11.2",
    "numpy>=2.4.6",
    "pandas>=3.0.6",
    "scikit-learn>=1.9.1",
    "tensorflow>=2.21.0",
]

[project.scripts]
projeto-qualiai = "projeto_qualiai:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
